# RAN1_119.yaml
# RAN1 #119 미팅 설정 파일
#
# Note: Final Minutes가 없어서 Draft Minutes 사용

meeting:
  # 기본 정보
  id: "RAN1_119"
  number: "119"
  working_group: "RAN1"
  folder_name: "TSGR1_119"

  # 입력 파일 경로 (paths.data.meetings 기준 상대 경로)
  # Note: Final Minutes 미제공으로 Draft Minutes 사용
  input:
    final_minutes: "meetings/RAN1/TSGR1_119/Report/Draft_Minutes_report_RAN1%23119_v030/Draft_Minutes_report_RAN1#119_v030.docx"
    tdoc_list: ""  # TBD

  # 출력 파일 설정
  output:
    filename_pattern: "{meeting_id}_{content_type}_{suffix}.md"

  # 미팅 특화 힌트 (선택사항)
  hints:
    # Expected counts (TBD - Ground Truth 없음)
    expected_counts:
      incoming_ls:
        primary: 0  # Unknown
        cc_only: 0  # Unknown
        total: 0    # Unknown

    # Source Working Groups (예상)
    source_wgs:
      - "RAN2"
      - "RAN3"
      - "RAN4"
      - "SA2"
